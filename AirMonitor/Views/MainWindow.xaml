<Window x:Class="AirMonitor.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AirMonitor.Views"
        xmlns:oxy="http://oxyplot.org/wpf"
        xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
        xmlns:mapsui="clr-namespace:Mapsui.UI.Wpf;assembly=Mapsui.UI.Wpf"
        mc:Ignorable="d"
        Height="900"
        Width="1400"
        MinWidth="1000"
        MinHeight="800"
        WindowStartupLocation="CenterScreen"
        Icon="/Assets/icon.ico"
        Title="AirMonitor - Program do analizy danych z modułu wykrywającego cząstki chemiczne w powietrzu">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Menu Grid.Row="0">
            <MenuItem Header="Program">
                <MenuItem Header="Konfiguracja"
                          Click="MenuItemConfiguration_Click" />
            </MenuItem>
        </Menu>


        <svgc:SvgViewbox Grid.Row="1"
                            Width="250"
                         Source="/Assets/logo.svg"
                         Margin="10,10,50,0"
                         HorizontalAlignment="Left"/>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    Margin="10">

            <Button Content="Wczytaj dane (CSV)"
                    Height="30"
                    Padding="10,0"
                    Margin="5"
                    x:Name="ImportData"
                    Click="ImportData_Click" />
            <Button Content="Eksportuj PDF"
                    Height="30"
                    Padding="10,0"
                    Margin="5"
                    x:Name="ExportPDF"
                    Click="ExportPDF_Click" />
            <Button Content="Podgląd Mapy"
                    Height="30"
                    Padding="10,0"
                    Margin="5"
                    x:Name="MapPreview"
                    Click="MapPreview_Click" />
        </StackPanel>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    Margin="10"
                    x:Name="ChemicalRadioPanel">
            <StackPanel.Resources>
                <Style TargetType="RadioButton">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="RadioButton">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="Gray"
                                        BorderThickness="1"
                                        CornerRadius="50"
                                        Padding="10">
                                    <ContentPresenter HorizontalAlignment="Center"
                                                      VerticalAlignment="Center" />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsChecked"
                                             Value="True">
                                        <Setter Property="Background"
                                                Value="DarkCyan" />
                                        <Setter Property="Foreground"
                                                Value="White" />
                                    </Trigger>
                                    <Trigger Property="IsMouseOver"
                                             Value="True">
                                        <Setter Property="Background"
                                                Value="Teal" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Background"
                            Value="LightGray" />
                    <Setter Property="Foreground"
                            Value="Black" />
                    <Setter Property="Cursor"
                            Value="Hand" />
                </Style>
            </StackPanel.Resources>
            <RadioButton Content="HCHO"
                         Width="100"
                         Height="100"
                         GroupName="ChemicalCompound"
                         Margin="5"
                         IsChecked="True"
                         Checked="RadioButton_Checked" />
            <RadioButton Content="HCL"
                         Width="100"
                         Height="100"
                         GroupName="ChemicalCompound"
                         Margin="5"
                         Checked="RadioButton_Checked" />
            <RadioButton Content="H2S"
                         Width="100"
                         Height="100"
                         GroupName="ChemicalCompound"
                         Margin="5"
                         Checked="RadioButton_Checked" />
            <RadioButton Content="NH3"
                         Width="100"
                         Height="100"
                         GroupName="ChemicalCompound"
                         Margin="5"
                         Checked="RadioButton_Checked" />
            <RadioButton Content="PM1"
                         Width="100"
                         Height="100"
                         GroupName="ChemicalCompound"
                         Margin="5"
                         Checked="RadioButton_Checked" />
            <RadioButton Content="PM2.5"
                         Width="100"
                         Height="100"
                         GroupName="ChemicalCompound"
                         Margin="5"
                         Checked="RadioButton_Checked" />
            <RadioButton Content="PM10"
                         Width="100"
                         Height="100"
                         GroupName="ChemicalCompound"
                         Margin="5"
                         Checked="RadioButton_Checked"
                         x:Name="LastRadioButton" />
        </StackPanel>
        
        <mapsui:MapControl Grid.Row="4"  x:Name="MapControl" Visibility="Hidden" />
        <oxy:PlotView Grid.Row="4"
                      Margin="10" 
                      x:Name="DataPlot"
                      Visibility="Visible"/>
    </Grid>
</Window>
